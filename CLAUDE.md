@AGENTS.md

## Claude Code only
- Run `Scripts/build-app.sh` after every Swift or web change before claiming a task done.
- Use `tail -f /tmp/corder.log` to verify runtime behaviour, not just compiler success.
